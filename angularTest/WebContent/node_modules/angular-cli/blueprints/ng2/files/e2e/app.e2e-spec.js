"use strict";
jsComponentName %  > Page;
from;
'./app.po';
describe('<%= htmlComponentName %> App', function () {
    var page =  %  > Page;
    beforeEach(function () {
        page = new  < ;
        jsComponentName %  > Page();
    });
    it('should display message saying app works', function () {
        page.navigateTo();
        expect(page.getParagraphText()).toEqual('<%= prefix %> works!');
    });
});
